version: 2.1

jobs:
  build:
    docker:
      - image: cimg/python:3.10.0
    steps:
      - checkout
      - run: python --version
      - setup_remote_docker
      - run:
          name: Build Docker image
          command: docker build -t aws-billing-to-slack:latest .
